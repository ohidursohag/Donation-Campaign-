import { createBrowserRouter } from "react-router-dom";

const myCreatedRoutes = createBrowserRouter([
   {
      path: '/',
      element: <div>Helo Donation</div>
   }
])

export default myCreatedRoutes